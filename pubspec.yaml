name: flutter_challenge
description: A new Flutter project for the code challenge of Appinio.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter_bloc: ^7.2.0
  flutter_riverpod: ^0.14.0+3
  geocoding: ^2.0.1
  geolocator: ^7.6.1
  http: ^0.13.3
  location: ^4.3.0
  permission_handler: ^8.1.6
  provider: ^6.0.0
  riverpod: ^0.14.0+3

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
